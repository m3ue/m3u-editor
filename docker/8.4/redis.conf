bind 0.0.0.0
port ${REDIS_SERVER_PORT}
timeout 0

# Suppress memory overcommit warning by disabling background operations
stop-writes-on-bgsave-error no
# Disable all background saves to avoid memory overcommit issues
save ""
# Disable RDB persistence completely
rdbcompression no
rdbchecksum no
# Only log warnings and errors — suppresses Redis 8.x module loading chatter
loglevel warning
# Disable syslog to avoid system-level warnings
syslog-enabled no
# Disable COW warning for ARM64 architecture since we're not using COW
ignore-warnings ARM64-COW-BUG
